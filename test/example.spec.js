//const { expect } = require('chai');
import { expect } from 'chai';

describe('TESTS', () => {
    it('Verify math sum', () => {
        expect(1 + 2).to.equal(3);
    });
    it('Verify math dev', () => {
        expect(8 / 2).to.equal(4);
    });
});
